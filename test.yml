- name: Playbook test
  hosts: front
  become: yes

  tasks:

    - name: Show message
      debug:
        msg: "Testing, testing..."

    - name: Create user
      user: 
        name: Pepe
        create_home: yes
